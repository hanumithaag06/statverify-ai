version: '3.8'

services:
  statverify-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: statverify_ai_app
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
